-- Tabelas do recurso. Executado no start pelo server/db.lua (uma instrução por ';').
-- Use sempre CREATE TABLE IF NOT EXISTS: o arquivo roda a cada start do recurso.
-- Não escreva ';' dentro de comentários — eles são removidos antes do split, mas
-- manter a regra evita surpresas ao editar.

-- CREATE TABLE IF NOT EXISTS nome_recurso_exemplo (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     citizenid VARCHAR(50) NOT NULL,
--     createdAt TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
-- ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
